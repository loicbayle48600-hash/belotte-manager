# Dépendances runtime (Windows + Linux). MetaTrader5 n'existe que sur Windows x64.
pyyaml>=6.0
numpy>=1.26
pandas>=2.1
anthropic>=0.40
mcp>=1.0
MetaTrader5>=5.0.45; sys_platform == "win32"
# Tests
pytest>=8.0
